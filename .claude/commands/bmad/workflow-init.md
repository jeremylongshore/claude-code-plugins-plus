Initialize the BMAD method in the current project. Check for the _bmad/ directory structure, create project configuration if needed, and display a welcome message with getting started instructions. Read _bmad/_config/manifest.yaml to verify the installation, then assess the current project state by scanning _bmad-output/ for existing artifacts. Report the project level (0-4) and recommend the first workflow to run.
