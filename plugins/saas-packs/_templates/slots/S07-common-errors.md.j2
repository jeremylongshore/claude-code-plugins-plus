---
name: {{ company }}-common-errors
description: |
  Diagnose and fix {{ display_name }} common errors and exceptions.
  Use when encountering {{ display_name }} errors, debugging failed requests,
  or troubleshooting integration issues.
  Trigger with phrases like "{{ company }} error", "fix {{ company }}",
  "{{ company }} not working", "debug {{ company }}".
allowed-tools: Read, Grep, Bash(curl:*)
version: 1.0.0
license: MIT
author: Jeremy Longshore <jeremy@intentsolutions.io>
compatible-with: claude-code
tags: [saas, {{ company }}]
---

# {{ display_name }} Common Errors

## Overview
{% if category in ('ai-ml',) %}
Quick reference for the most common {{ display_name }} API errors: authentication failures, rate limits, context length issues, and model availability.
{% elif category in ('data-platform',) %}
Quick reference for the most common {{ display_name }} errors: connection failures, query timeouts, permission issues, and schema problems.
{% elif category in ('sales-crm',) %}
Quick reference for the most common {{ display_name }} errors: duplicate records, rate limits, validation failures, and sync conflicts.
{% elif category in ('communication',) %}
Quick reference for the most common {{ display_name }} errors: delivery failures, suppressed recipients, domain verification, and template issues.
{% else %}
Quick reference for the top 10 most common {{ display_name }} errors and their solutions.
{% endif %}

## Prerequisites
- {{ display_name }} SDK installed
- API credentials configured
- Access to error logs

## Instructions

### Step 1: Identify the Error
Check error message and code in your logs or console.

### Step 2: Find Matching Error Below
Match your error to one of the documented cases.

### Step 3: Apply Solution
Follow the solution steps for your specific error.

## Output
- Identified error cause
- Applied fix
- Verified resolution

## Error Handling

### {{ error_1_name | default('Authentication Failed') }}
**Error Message:**
```
{{ error_1_message | default('Authentication error: Invalid API key') }}
```

**Cause:** {{ error_1_cause | default('API key is missing, expired, or invalid.') }}

**Solution:**
```bash
{{ error_1_solution | default('# Verify API key is set\necho $' + company | upper + '_API_KEY') }}
```

---

### {{ error_2_name | default('Rate Limit Exceeded') }}
**Error Message:**
```
{{ error_2_message | default('Rate limit exceeded. Please retry after X seconds.') }}
```

**Cause:** {{ error_2_cause | default('Too many requests in a short period.') }}

**Solution:**
{{ error_2_solution | default('Implement exponential backoff. See `' + company + '-rate-limits` skill.') }}

---

### {{ error_3_name | default('Network Timeout') }}
**Error Message:**
```
{{ error_3_message | default('Request timeout after 30000ms') }}
```

**Cause:** {{ error_3_cause | default('Network connectivity or server latency issues.') }}

**Solution:**
```{{ primary_language | default('typescript') }}
{{ error_3_solution | default('// Increase timeout\nconst client = new Client({ timeout: 60000 });') }}
```

{% if category in ('ai-ml',) %}
---

### Content Moderation / Safety Filter
**Error Message:**
```
400 Bad Request: Content policy violation — output blocked by safety filter
```

**Cause:** Model output triggered content policy filters or input contained restricted content.

**Solution:**
Review your system prompt for policy-compliant framing. Use moderation endpoint to pre-check inputs. Adjust temperature/prompts to reduce edge-case outputs.
{% elif category in ('data-platform',) %}
---

### Deadlock Detected
**Error Message:**
```
ERROR: deadlock detected — Process 12345 waits for ShareLock on transaction 67890
```

**Cause:** Two concurrent transactions waiting on each other's locks. Common with concurrent UPDATEs on same rows.

**Solution:**
Ensure consistent lock ordering across transactions. Use `SELECT ... FOR UPDATE SKIP LOCKED` for queue patterns. Reduce transaction scope and duration.
{% elif category in ('sales-crm',) %}
---

### Duplicate Record on Import
**Error Message:**
```
409 Conflict: Contact with email user@example.com already exists (id: abc-123)
```

**Cause:** Attempting to create a record with an email/phone that matches an existing contact.

**Solution:**
Use the upsert endpoint instead of create: `client.contacts.upsert({ email, ...data })`. Or search first and merge if found.
{% endif %}

## Examples

### Quick Diagnostic Commands
```bash
# Check {{ display_name }} status
curl -s {{ status_url | default('https://status.' + company + '.com/api/status') }}

# Verify API connectivity
curl -I {{ api_url | default('https://api.' + company + '.com') }}

# Check local configuration
env | grep {{ company | upper }}
```

### Escalation Path
1. Collect evidence with `{{ company }}-debug-bundle`
2. Check {{ display_name }} status page
3. Contact support with request ID

## Resources
- [{{ display_name }} Status Page]({{ status_url | default('https://status.' + company + '.com') }})
- [{{ display_name }} Support]({{ docs_url | default('https://docs.' + company + '.com') }}/support)
- [{{ display_name }} Error Codes]({{ docs_url | default('https://docs.' + company + '.com') }}/errors)

## Next Steps
For comprehensive debugging, see `{{ company }}-debug-bundle`.
