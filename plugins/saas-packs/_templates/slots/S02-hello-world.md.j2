---
name: {{ company }}-hello-world
description: |
  Create a minimal working {{ display_name }} example.
  Use when starting a new {{ display_name }} integration, testing your setup,
  or learning basic {{ display_name }} API patterns.
  Trigger with phrases like "{{ company }} hello world", "{{ company }} example",
  "{{ company }} quick start", "simple {{ company }} code".
allowed-tools: Read, Write, Edit
version: 1.0.0
license: MIT
author: Jeremy Longshore <jeremy@intentsolutions.io>
compatible-with: claude-code
tags: [saas, {{ company }}]
---

# {{ display_name }} Hello World

## Overview
{% if category in ('ai-ml',) %}
Send your first prompt to the {{ display_name }} API and get a model response back.
{% elif category in ('data-platform',) %}
Run your first query against {{ display_name }} and read actual data back.
{% elif category in ('developer-tools',) %}
List your projects and trigger your first action via the {{ display_name }} API.
{% elif category in ('design-creative',) %}
Connect to {{ display_name }} and list your design files via the API.
{% elif category in ('sales-crm',) %}
Pull your first contacts from {{ display_name }} and display them.
{% elif category in ('communication',) %}
Send your first test message through the {{ display_name }} API.
{% elif category in ('productivity',) %}
List your workspace pages and read content from {{ display_name }}.
{% elif category in ('fintech',) %}
Check your account balance and list recent transactions via {{ display_name }}.
{% elif category in ('infra-ops',) %}
List your monitored projects and check system status via {{ display_name }}.
{% else %}
Minimal working example demonstrating core {{ display_name }} functionality.
{% endif %}

## Prerequisites
- Completed `{{ company }}-install-auth` setup
- Valid API credentials configured
- Development environment ready

## Instructions

### Step 1: Create Entry File
Create a new file for your hello world example.

### Step 2: Import and Initialize Client
```{{ primary_language | default('typescript') }}
import { {{ client_class | default(display_name + 'Client') }} } from '{{ npm_package | default(company + '-sdk') }}';

const client = new {{ client_class | default(display_name + 'Client') }}({
{% if category in ('data-platform',) %}
  connectionString: process.env.{{ company | upper }}_DATABASE_URL,
{% elif category in ('design-creative',) %}
  accessToken: process.env.{{ company | upper }}_ACCESS_TOKEN,
{% else %}
  apiKey: process.env.{{ company | upper }}_API_KEY,
{% endif %}
});
```

### Step 3: Make Your First API Call
```{{ primary_language | default('typescript') }}
async function main() {
  {{ hello_world_ts | default('// Your first API call here') }}
}

main().catch(console.error);
```

## Output
- Working code file with {{ display_name }} client initialization
- Successful API response confirming connection
- Console output showing:
```
{{ expected_output | default('Success! Your ' + display_name + ' connection is working.') }}
```

## Error Handling
| Error | Cause | Solution |
|-------|-------|----------|
| Import Error | SDK not installed | Verify with `npm list` or `pip show` |
| Auth Error | Invalid credentials | Check environment variable is set |
| Timeout | Network issues | Increase timeout or check connectivity |
| Rate Limit | Too many requests | Wait and retry with exponential backoff |

## Examples

### TypeScript Example
```typescript
import { {{ client_class | default(display_name + 'Client') }} } from '{{ npm_package | default(company + '-sdk') }}';

const client = new {{ client_class | default(display_name + 'Client') }}({
{% if category in ('data-platform',) %}
  connectionString: process.env.{{ company | upper }}_DATABASE_URL,
{% elif category in ('design-creative',) %}
  accessToken: process.env.{{ company | upper }}_ACCESS_TOKEN,
{% else %}
  apiKey: process.env.{{ company | upper }}_API_KEY,
{% endif %}
});

async function main() {
  {{ hello_world_ts | default('// Your first API call here') }}
}

main().catch(console.error);
```

### Python Example
```python
from {{ pip_package | default(company) }} import {{ client_class | default(display_name + 'Client') }}

client = {{ client_class | default(display_name + 'Client') }}()

{{ hello_world_py | default('# Your first API call here') }}
```

## Resources
- [{{ display_name }} Getting Started]({{ docs_url | default('https://docs.' + company + '.com') }}/getting-started)
- [{{ display_name }} API Reference]({{ docs_url | default('https://docs.' + company + '.com') }}/api)
- [{{ display_name }} Examples]({{ docs_url | default('https://docs.' + company + '.com') }}/examples)

## Next Steps
Proceed to `{{ company }}-local-dev-loop` for development workflow setup.
