---
name: {{ company }}-install-auth
description: |
  {{ display_name }} installation and authentication setup. Trigger phrases:
  "install {{ company }}", "setup {{ company }}", "{{ company }} auth",
  "configure {{ company }} API key", "{{ company }} SDK setup".
allowed-tools: Read, Write, Bash, Grep
version: 1.0.0
license: MIT
author: Jeremy Longshore <jeremy@intentsolutions.io>
---

# {{ display_name }} Install & Auth

## Overview
Set up {{ display_name }} SDK/CLI and configure authentication credentials.

## Prerequisites
- Node.js 18+ or Python 3.10+
- Package manager (npm, pnpm, or pip)
- {{ display_name }} account with API access

## Installation Steps

### 1. Install SDK
```bash
# Node.js
npm install {{ npm_package | default(company + '-sdk') }}

# Python
pip install {{ pip_package | default(company) }}
```

### 2. Configure Authentication
```bash
# Set environment variable
export {{ company | upper }}_API_KEY="your-api-key"

# Or create .env file
echo '{{ company | upper }}_API_KEY=your-api-key' >> .env
```

### 3. Verify Connection
```{{ primary_language | default('typescript') }}
{{ verification_code | default('// Test connection code here') }}
```

## Common Issues
- **Invalid API Key**: Verify key in {{ display_name }} dashboard
- **Rate Limited**: Check quota at {{ docs_url | default('docs.' + company + '.com') }}
- **Network Error**: Ensure firewall allows outbound HTTPS

## Next Steps
After successful auth, proceed to `{{ company }}-hello-world` for your first API call.
