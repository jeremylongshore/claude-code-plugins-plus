-- Current future grants for one validated database identifier. The collector
-- replaces __DATABASE_IDENTIFIER__ only after strict identifier validation.
SHOW FUTURE GRANTS IN DATABASE __DATABASE_IDENTIFIER__ LIMIT 10000;
